# Mini Training Framework 依赖
# 目标环境: 8×H20 GPU, CUDA 12.1+

# 核心
torch>=2.1.0
# flash-attn>=2.3.0  # 需要单独编译: pip install flash-attn --no-build-isolation

# 分布式训练框架
deepspeed>=0.12.0

# 配置和监控
pyyaml>=6.0
tensorboard>=2.14.0
# wandb>=0.15.0  # 可选: 实验追踪

# 性能分析
nvidia-ml-py3>=7.352.0

# 开发工具
pytest>=7.0.0
